{
  "name": "cypress_at",
  "version": "1.0.0",
  "description": "cypress_auto_test_for_boohoo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser chrome",
    "cy:headless": "cypress run --browser chrome --spec",
    "cy:headed": "cypress run --browser chrome --headed --spec"
  },
  "devDependencies": {
    "cypress": "^13.3.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1"
  }
}
